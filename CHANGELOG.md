# Changelog

## 0.3.8（2026-09-25：四轮迭代完善批次）

### 缺陷修复（第 1 轮）
- 修复 Neow 加血补间断：实时模式（0.3.2 起默认）下顶栏在整个 Neow 事件期间显示 `0/最大HP`；
  现在仅在「血量/金币失忆模式」开启时抑制原版补间（新增 Core 纯规则 `StatusDisplayRules` + 单测）
- 修复文本恢复哨兵：`TextBlurService.Restore` 不再把原文写入乱码输出元数据，避免
  `BlurNode` 永久短路导致地图图例「显示所有路线」关→开→关后不再乱码
- 版本号不再手工同步：新增四处（csproj / 根 manifest / 工坊 manifest / CHANGELOG）一致性测试

## 0.3.7（2026-09-25：认知修改器「关闭脑雾尖塔」开关）

### Mod 关闭开关（2026-09-25 用户需求）
- 认知修改器标题栏下方新增「**关闭脑雾尖塔（本 mod 不再影响游戏）**」勾选项
- 勾选后本 mod 完全停止生效：所有乱码、卡牌黑雾与记忆规则、遗物/敌人/地图/意图/状态遮蔽、低血量提示立即恢复原版呈现
- 关闭期间面板其余选项隐藏，仅保留该开关与说明，可随时取消勾选恢复（恢复时按当前设置重新应用）
- 关闭状态持久化于 `settings.cfg` 的 `[mod] disabled`，跨重进保留；联机自动禁用逻辑不变（联机时面板整体隐藏）

## 0.3.6（2026-09-21：移除卡牌计数器）

### 移除卡牌计数器（2026-09-21 用户决定）
- 认知修改器不再提供「卡牌计数器」选项，右上角出牌计数排行榜一并移除
- 坏记性模式下新获得牌「先认得」的行为保留（原计数器附带的编号/持久化数据删除）
- 旧存档里的计数器数据不再读取，也不影响其它功能

## 0.3.5（2026-09-21：正式服 v0.107.1 适配 + Boss/图例悬停描述纳入文字乱码）

### 正式服适配（2026-09-21 用户追加）
- 同一个 `BrainFog.dll` 现在同时支持**正式服 v0.107.1** 与**测试服 v0.111.0**：`min_game_version` 降至 0.107.1，RitsuLib 编译目标切到 0.107.1（其 0.107.1/0.111.0 变体 mod API 一致）
- 验证改为双版本：`tools/check.sh` 分别对 v0.107.1 与 v0.111.0 引用程序集编译并跑补丁目标审计；`tools/fetch-refs.sh` 拉取 MegaCrit 授权的 NuGet 引用包（`.refs/sts2-refs/`）
- 兼容修复：`MegaSpineBinding` 在 0.107.1 尚未实现 `IDisposable`，呼吸方框的 Spine 轨道改为 `try/finally` 兼容两版

### Boss/图例悬停描述跟随文字乱码（2026-09-21 用户追加）
- 开启「显示地图所有路线」后，顶栏阶段 Boss 图标与地图图例的**悬停描述**不再豁免乱码，改为跟随「文字」模块的乱码百分比（0% 可读 / 100% 全乱码）
- 图标、图例标签文字与地图 Boss 立绘的恢复行为不变（仍由该选项控制）

## 0.3.4（2026-09-21：「显示地图所有路线」恢复阶段 Boss 信息与图例）

### 地图信息恢复（2026-09-21 用户追加）
- 开启「显示地图所有路线」时，顶栏恢复原版**阶段 Boss 图标**及其悬停描述（「本阶段最强的敌人……」），图标可聚焦、按房间规则显示/隐藏
- 同选项下地图**图例**（标题与各条目文字）恢复原版可读，不再随统一比例乱码；悬停图例/Boss 图标时显示原版描述
- 关闭选项时恢复原遮蔽：顶栏图标隐藏、图例文字随统一比例乱码
- 切换即时生效（地图打开中也刷新）

## 0.3.3（2026-09-21：恢复原版地图画图功能）

### 地图画图恢复原版（2026-09-21 用户改版）
- 恢复原版地图画图：画线/擦除按钮、右键绘制与画线快捷键不再被隐藏或拦截，画图工具面板保持原版显示
- 地图迷雾仅影响节点与路线的可见性（0.3.2 起默认全路线可见；关闭「显示地图所有路线」后仅当前/已走/下一层可见），不影响画图
- 该行为不纳入修改器选项，始终为原版行为

## 0.3.2（2026-09-21：默认值调整 + 「当前设置为默认」按钮）

### 默认值调整（2026-09-21 用户改版）
- 卡牌奖励揭示：不揭示 → **全部揭示**
- 商店/事件卡面揭示：否 → **是**
- 血量数/金币数恢复正常显示：否 → **是**
- 显示遗物：否 → **是**
- 显示地图所有路线：否 → **是**
- 敌人模型可见：否 → **是**
- 可见敌人意图：不可见 → **可见所有意图**
- 不变：乱码 50%、卡牌记忆「坏记性」、n=2、记忆消逝开、卡牌计数器关、血量/金币失忆模式关
- 说明：已保存的旧配置保留玩家现值；「重置为默认」后应用新默认（未自定义默认时）

### 修改器：「当前设置为默认」按钮（2026-09-21 用户追加）
- 在「重置为默认」上方新增「**当前设置为默认**」：把当前修改器设置保存为默认（持久化于 settings.cfg 的 `defaults` 段，跨重进保留，按保存时的快照整体覆盖）
- 「重置为默认」恢复目标改为：玩家保存的自定义默认（若从未保存则用出厂默认）
- 未显式设置过的选项在读取配置时以"当前默认"为回退（自定义默认优先于出厂默认）
- 按下后有「已保存为默认」短暂反馈

## 0.3.1（2026-09-21：「显示遗物」全场景恢复 + 记忆消逝走原版删卡动画）

### 「显示遗物」（原「显示已拥有遗物」，2026-09-21 追加）
- 感知模块选项更名为「**显示遗物**」并扩大范围：开启后恢复**所有**遗物显示——已拥有（库存/检视）、遗物奖励行（图标 + 真实名称，不再显示"未知遗物"）、商店、宝箱、跑图历史
- 悬停提示同步：开启后宝箱/商店/奖励/库存/通用遗物架的悬停显示真实名称与描述；关闭时保持原有全遮蔽
- 切换即时生效（含已打开的检视界面与奖励行）
- 配置键 `show_owned_relics` → `show_relics`（旧值自动沿用；重置为默认后为关）

### 记忆消逝：原版删卡动画（2026-09-21 追加）
- 战斗结束移除未揭示卡牌时改走游戏原版 `CardPileCmd.RemoveFromDeck`：播放卡片预览与飞散特效（`NCardRemoveVfx`），并触发遗物的 `BeforeCardRemoved` 钩子；未揭示的牌预览为黑雾，不泄漏牌面

## 0.3.0（2026-09-21 已上架创意工坊：六项认知规则扩展，实机验收通过）

### 初始卡组揭示（2026-09-21）
- 每局开局时初始卡组处于揭示状态：好记性模式按定义揭示（整张卡本局已知），坏记性模式按副本揭示（之后仍会失忆）；通晓万物本就全可见，歪比巴卜保持永不揭示

### 感知模块：敌人模型可见（2026-09-21）
- 新增开关「敌人模型可见」（默认关，保持默认只显示呼吸方框）：开启后敌人显示真实模型，切换即时生效（敌人名字仍隐藏）

### 坏记性调整（2026-09-21）
- n 的默认值 1 → 2（已保存的旧配置保留原值；「重置为默认」后为 2）
- 坏记性模式下新加入卡组的卡牌初始处于揭示状态（奖励/商店/事件/转化等所有入组途径；之后仍按 n 次上手规则失忆）

### 记忆消逝（2026-09-21，卡牌记忆设置，默认开启）
- 每场战斗结束时，处于未揭示状态的卡牌从卡组中移除：好记性按定义、坏记性按副本判定「失忆」；歪比巴卜模式不受影响
- 不保底：卡组可能被删空；移除会记入本局历史并随存档持久化（揭示/坏记性计数/计数器条目同步清理）

### 坏记性失忆提醒（2026-09-21）
- 坏记性模式下，手牌中「本回合不打出就会失忆」的已揭示卡牌牌面变暗；打出或离开手牌后恢复

### 卡牌计数器与出牌计数排行榜（2026-09-21，默认关闭）
- 记录本局每张牌（按副本）打出的次数；同名副本按加入卡组先后顺序标定固定序号（打击1/打击2…；移除后不重排、新卡往后续编）
- 开启后在右上角出现「出牌计数」排行榜（按次数降序，仅列出打出≥1次的副本），标题栏可收起/展开（状态持久化）
- 计数与编号随存档持久化；移除的牌从榜单消失

## 0.2.6（2026-09-21 已上架创意工坊：修复 0.2.5 数值可读开关误还原其它乱码文本）

### 修复（2026-09-21）
- 修复：切换「血量数/金币数恢复正常显示」时，**主标题界面等由专用补丁负责乱码的文本**被错误还原为正常文字（`TextBlurService.ReapplyAllText` 误用通用豁免判定；现仅还原该选项覆盖的 HP/金币数值标签，其余保持乱码）

## 0.2.5（2026-09-21 已上架创意工坊：感知模块新增「血量数/金币数恢复正常显示」）

### 血量/金币数值可读开关（2026-09-21）
- 感知模块新增开关「**血量数/金币数恢复正常显示**」（默认关）：开启后**顶栏血量数、顶栏金币数与自己战斗血条的血量数**不再乱码
- 只豁免数值本身：实时/失忆模式不受影响；格挡数字、金币飘字、敌人血条数字仍按统一比例乱码
- 切换即时生效（开启立刻还原为可读，关闭立刻重新乱码）；随配置持久化（`readable_status_numbers`）

## 0.2.4（2026-09-21 已上架创意工坊：9.20 任务——模块化修改器 / 卡牌记忆模式 / 乱码模式 / 默认值 / 事件名修复）

### 认知修改器模块化 + 主标题界面可用（2026-09-21，9.20 任务）
- 面板按「文字 / 认知 / 感知」三模块整理，**主标题界面即可操作**（不再仅限对局内；对局内照旧）
- 认知模块 = **获得卡牌认知设置**（卡牌奖励 5 档：不揭示 / 随机揭示1张 / 随机揭示2张 / 随机揭示3张 / 卡牌奖励全部揭示；商店/事件卡面 是/否）+ **卡牌记忆设置**（4 模式）
- 感知模块：「血量/金币快照模式」更名「**血量/金币失忆模式**」；「可见敌人意图」改为 3 选项（不可见 / 仅第一回合可见 / 可见所有意图）
- 新增「**重置为默认**」按钮；设置跨重进保留（配置 `memory_mode`/`bad_memory_n`/`intent_visibility`/`blur_salt_mode` 等；旧版配置自动落到新默认）
- 选项说明从面板底部一行改为**选项旁的悬浮卡片**（更大字号 16、深色圆角+金边，面板右侧显示，越界自动翻到左侧）

### 卡牌记忆四模式（2026-09-21，9.20 任务）
- **通晓万物**：全部卡牌全局揭示
- **好记性**：打出/升级一张 → 该定义本局永久揭示；同名卡在**所有获得界面（奖励/商店/事件）**也显示真牌面（优先级高于获得卡牌设置）
- **坏记性**（默认，n 默认 1、可调 ≥1）：按副本记忆；某张已揭示卡**每 n 次上手却未被打出**即变回未揭示（自然抽牌与效果回手都算；打出重置计数；计数随存档持久化，SL 保留）
- **歪比巴卜**：卡牌永不揭示（优先级高于获得卡牌设置）
- 旧「同名卡全部揭露 / 卡牌本局全部揭示」开关移除，由模式选择取代

### 乱码模式（2026-09-21 追加）
- 文字模块新增「**乱码模式**」：固定混乱（乱码不随重进变化）/ 混乱混乱（每次重进重新随机，默认）；切换即时重应用（`BlurSalt` 固定盐 0 或进程盐）

### 事件名称乱码修复（2026-09-21，9.20 任务）
- 根因：`NEventLayout.SetTitle` 直接赋值 `_title.Text`，绕过 `MegaLabel.SetTextAutoSize` 源头乱码，事件名保持可读；改为经 `SetTextAutoSize` 路由后随统一比例乱码

### 统一乱码百分比滑块（2026-09-21）
- 所有文本（含此前分档的卡面/对话/事件/菜单/悬停等）统一为一个「乱码百分比」：滑块 **0%–100%，每 1% 一档**，默认 50%
- 滑块拖动**即时重应用**到已显示文本（节点保存原文本 `TextBlurService.InputMeta` + `ReapplyAllText`）；0% = 完全无乱码
- 豁免保持可读：敌人意图（数字/悬停提示）、设置界面、卡牌图鉴、模组面板自身
- 事件文本改为统一源头处理（原事件前缀乱码移除，仅保留选项图标可见性规则）；悬停提示统一比例（意图提示仍豁免）
- `TextBlurPercents` 仅保留默认值 50；删除已无用的 `PotionTextBlurrer`

### 血量/金币默认实时 + 失忆模式改面板选项（2026-09-21）
- 默认改为**实时**：顶栏 HP/金币随受伤/加钱即时更新，但**数值按统一百分比乱码**；战斗内自己血条恢复显示（数字乱码）
- 「失忆模式」为面板开关（默认关）：开启后回到旧行为——停留在上次休息时、灰显+标注、战斗血条隐藏真实 HP、休息/扣钱刷新
- 失忆数据在实时模式下也会在休息/扣钱时保持最新，切换模式即时正确
- 214 测试全绿

## 0.2.3（2026-09-21 已上架创意工坊：源头乱码 / 受击数字乱码 / 事件获得稀有度 / 全揭示 / 边缘缩进 / SL 揭示保留）

### 全局文本乱码改为源头改写（2026-09-21）
- 通用 UI 文本改为在 `MegaLabel.SetTextAutoSize` / `MegaRichTextLabel.SetTextAutoSize` 前缀统一乱码（`GlobalTextBlurSource` + Core `GlobalTextBlurRules`），取代 0.2s 场景扫描：新文本即时乱码，不再有"最多 0.2~0.4s 可读"的窗口，打牌热路径也无需全树扫描
- 周期扫描仅保留为**非 Mega 控件**（少数原生 `Label`/`RichTextLabel`）的兜底；Mega 子树整棵跳过
- 上下文规则与原扫描一致：卡面/悬停提示/事件/菜单/对话/设置/图鉴/模组面板各自由原补丁处理；顶栏与地图描述 70%，其余默认 60%
- **受击/治疗/格挡数字改为乱码**（用户规则 2026-09-21，撤销"受击数字保留可读"）：`NDamageNumVfx`/`NHealNumVfx`/`NDamageBlockedVfx` 按默认 60% 乱码；其余 VFX 文本仍保持可读
- 敌人意图保持可读：`NIntent` 子树豁免（意图数字），意图悬停提示继续豁免（行动/攻击说明不乱码）

### 事件获得卡牌稀有度规则（2026-09-21）
- 卡牌选择屏幕在创建时打"获得场景"标记（`CardAcquireScopePatch`：`NChooseACardSelectionScreen.ShowScreen`、`NSimpleCardSelectScreen.Create` 两个重载）；`CardFogRenderer` 命中标记即按获得场景处理（仅稀有度边框），闭合 `IMPLEMENTATION-MAP` §2.3 偏差

### 认知修改器：卡牌本局全部揭示 + 边缘缩进（2026-09-21）
- 新增「卡牌本局全部揭示」：本局所有卡牌直接显示真实牌面（文字仍按规则乱码），设置项 `reveal_all_cards`
- 新增边缘缩进：标题栏 ◀/▶ 按钮把面板缩进到较近的屏幕左/右边缘，边缘留一个小按钮（▶/◀）点击弹出；缩进侧与垂直位置持久化（`docked`/`dock_side`/`dock_y`），原「收起/展开」保留
- 面板提示行更新；中/英键新增 `panel_reveal_all`/`panel_hint_reveal_all`/`panel_dock_tooltip`/`panel_tab_tooltip`

### 修复：SL（退出到主标题后重进）按副本揭示被重置（2026-09-21）
- **根因**：按副本模式（`reveal_same_name=false`）的实例 ID 虽已写进存档（`RevealedInstances` + `DeckOrderIds`），但读档后没有把 ID 重新绑定到新载入的牌组对象；渲染走 `TryGetId` 得到 null → 全部卡牌显示未知。实测当前存档：牌组 14 张、保存牌序 13 条（最后一张为后来获得）
- **修复**：`RunStarted` 时立即绑定——新增 `DeckOrderKeys`（每个牌序槽位的定义键），`Core/Reveal/DeckRebinder` 按定义键对齐（容忍新增/移除卡牌、重复定义按序匹配；重复/非法 ID fail-closed）；旧存档（无键）退回按索引绑定前 N 个槽位；绑定成功后关闭懒绑定，避免新卡抢占已保存槽位
- 新增 `DeckRebinderTests`（11 用例：插入/移除/重复定义/重复 ID/非法 ID/旧存档回退等）

## 0.2.2（2026-09-20 已上架创意工坊：每次启动随机乱码 + 修改器面板美化）

### 性能：打牌卡顿/音效延迟修复（2026-09-20）
- **根因**：旧实现在游戏写完卡面文字**之后**再覆盖为乱码。游戏的 `MegaLabel/MegaRichTextLabel` 只在「新字符串 ≠ 当前字符串」时才重排文本，于是每次 `UpdateVisuals`（打牌动画期间高频调用）都触发「真文本→重排→乱码→重排」双份 BBCode 解析与字号自适应，造成可听/可见的卡顿
- **修复**：改为**在源头乱码**（`CardFaceTextBlurPatch`：`MegaLabel.SetTextAutoSize` / `MegaRichTextLabel.SetTextAutoSize` 前缀改写传入字符串）。同文本重复赋值时游戏自身短路，不再重排；配合同输入→输出备忘（`InputMeta`/`OutputMeta`），热路径只剩一次元数据比对
- **全局文本扫描降载**：`GlobalTextBlurDriver` 增加自适应退避（本轮无文本变化则间隔 ×1.5，上限 0.4s；有变化立即回到 0.2s），并整棵跳过 VFX/粒子/拖尾子树——打牌特效生成/销毁不再触发全树遍历
- 附带修正：伤害数字/"Blocked"/治疗数字属于 VFX 子树，现不会被扫描（符合规格「受击效果保留含伤害数字」）

### 修复：按副本记忆的揭示每场战斗重置（2026-09-20）
- **根因**：游戏在战斗开始时把牌组每张牌**克隆**进抽牌堆（`Player.PopulateCombatState`），克隆体带 `DeckVersion` 指回本体；旧实现直接给克隆体发实例 ID，导致「揭示记录里的 ID」与「存档牌序里的 ID」永不重合（实测存档 9 个揭示 ID ∩ 11 个牌序 ID = 0），下一场战斗又是新克隆体 → 揭示丢失，表现为「每场战斗刷新」
- **修复**：实例身份沿 `DeckVersion`（战斗克隆）/ `CloneOf`（Anger、双持等效果克隆）回溯到本体（`Core/Reveal/CardIdentity.cs`，带 8 跳深度上限防环）；`CardInstanceRegistry` 的查询/注册/绑定全部先解析身份
- 揭示时同步刷新存档牌序，避免读档后索引漂移；`RevealSameNameCards` 关闭（按副本记忆）时现在可正确跨战斗、跨读档保留
- 新增 6 个身份解析单测（含自环/长环终止）；152 测试全绿

### 每次进入游戏都不一样（2026-09-20）
- **乱码随机化**：所有乱码（事件、菜单、提示、卡面、药水、对话，含**主标题界面**）改为**每次启动游戏重新随机**——同一次启动内保持稳定（同一界面不会重掷，玩家读得完），下次启动整体换一套图案
- 实现：`Core/Text/BlurSalt.cs`（进程级随机盐，`Random.Shared.Next()`）；`EventTextBlurrer.Blur(text, percent, salt)` 新增盐值重载；全部调用点（`TextBlurService`、事件补丁、`CardFogRenderer`、`PotionTextBlurrer`）接入
- 标题界面本就包含在菜单乱码规则内（75%）；本次改动使其每次启动都重新随机

### 认知修改器面板美化（2026-09-20）
- 面板改为圆角卡片（阴影、金色描边、加宽 252px）；标题金色，点击标题栏即可收起/展开（箭头 ▾/▸ 带悬停高亮）
- 内容分组：「认知」（选卡揭露/商店事件/同名卡）与「感知」（实时状态/遗物/地图/意图）两个小节，带分隔线
- 悬停任意选项时，底部提示行显示该选项说明；移开后恢复默认提示「拖动标题栏可移动 · 修改即时生效」
- **面板可拖动到任意位置**：按住标题栏拖动（光标为移动样式），位置按视口比例持久化到 `settings.cfg`（`panel_x`/`panel_y`），换分辨率/重启后仍在原处；拖动越界自动夹回屏幕内；标题栏点击（位移 < 8px）仍为收起/展开
- 修复：点击收起时偶发只隐藏文字、底框不变——`PanelContainer` 在容器外不会自动回缩，收起/展开后延迟一帧按内容重算尺寸并夹回屏幕内；箭头按钮点击不再与标题栏点击冲突（命中检测避免双触发）
- **面板跟随游戏语言**：游戏语言为中文（zhs）时显示中文，其他任意语言显示英文版（标题、分组、全部选项与悬停提示）；语言表新增 `panel_*` 键（`src/Runtime/Localization/zhs.json` + `eng.json`）；游戏内切换语言约 0.5s 内自动刷新（含 CJK 字体替换）

## 0.2.1（未发布：认知修改器更名 + 可见意图）

### 更名（2026-09-19）
- 面板「难度调整器」更名为「认知修改器」（面板标题同步）；代码类名与 `settings.cfg` 键保持不变（兼容旧配置）

### 可见敌人意图（新功能，仅对局内）
- 面板新增「可见敌人意图」开关：开启后敌人意图每回合持续可见（默认仍为仅战斗首回合）
- 实现：`IntentGate.ShouldShow` 增加开关短路；`DifficultyRefresh` 支持切换即时刷新（含入场生物）

### 可读性（用户规则 2026-09-20）
- **敌人意图保持可读**：意图数字/图标不被乱码（全局扫描豁免 `NIntent` 子树）；意图悬停提示（"攻势/濒死一击"等）也保持原文——否则「可见敌人意图」打开后攻击数字是乱的

## 0.2.0（未发布：实机验收调整 + 难度调整器）

### 难度调整器（新功能，2026-09-20）
- 画面左侧新增难度调整面板（仅对局内显示，可折叠；设置写入 `user://BrainFog/settings.cfg`）：
  - 选卡界面揭露：不揭露 / 随机1张 / 随机2张 / 随机3张 / 全部（卡牌奖励选择界面随机揭露，每次奖励固定）
  - 商店/事件卡面揭露：开关（商店与事件获得场景卡面）
  - 同名卡全部揭露：开关（关闭 = 只揭露打出的那一张，恢复按副本记忆 + 存档重绑）
  - 显示实时血量/金币：顶栏真实数值（隐藏灰显/快照标注），战斗血条恢复真实显示
  - 显示已拥有遗物：库存/检视可见（奖励/商店/宝箱仍遮蔽）
  - 显示地图所有路线：全节点路线可见，Boss 点恢复真实立绘与悬停（画线仍禁用）
- 设置变更即时刷新全场卡面
- 性能：打出牌揭示只在**首次**记录知识（重复打出为 no-op，不再重复写档/全量刷新）；卡面视觉刷新延迟一帧，避开打出动画起手；卡牌显示上下文按父节点缓存（避免每次 UpdateVisuals 重走祖先链）；实例模式去掉重复的牌序重建；`BRAINFOG_DEBUG=1` 下揭示耗时 >0.5ms 会输出 `[BrainFog][Perf]` 日志

### 实机验收调整（2026-09-19）
- 卡牌揭示改为**按定义**：打出/升级任一副本后，该卡（含同名其他副本、之后获得的）本局永久揭示；持久化简化为定义键集合，去掉牌序重绑（旧存档揭示等价重置）
- 敌人名字隐藏（悬停只显示血条/能力，不显示名字）
- 药水名称与描述 50% 固定乱码（同一药水每次一致）
- 泄漏修复第二轮：事件选牌/通用选牌（牌不在任何玩家牌堆）按获得场景仅显示稀有度边框；升级预览克隆不再构成揭示——奖励界面右键查看升级不露牌面
- 战斗内自己角色血条隐藏真实 HP（数字/填充/中毒/毁灭覆盖），仅顶栏旧快照；格挡保留
- 宝箱/商店遗物悬停提示抑制；遗物奖励行名称替换为"未知遗物"
- 标题界面与暂停菜单选项文字 75% 固定乱码（设置按钮保留可读）
- 第三轮：卡牌查看/升级界面的关键词描述（中毒/虚弱等）50% 固定乱码
- 第三轮：顶栏血量灰色显示（非真实状态），标注"上次休息时的状态"并附火堆刷新提示
- 第三轮：顶栏不再显示当前阶段末尾 Boss 图标（悬停提示同移除）
- 第四轮：统一乱码比例策略（`TextBlurPercents`）——未规定文本默认 60%
- 第四轮：揭示后卡面文字 85% 乱码（图鉴除外）；"战斗开始"横幅 60% 乱码
- 第四轮：顶栏/地图 UI 描述（生命值/金币/敌人图例/楼层/地图/牌组/分享/图例）70% 乱码，其余悬停提示默认 60%（设置/图鉴/药水除外）
- 第四轮：先古之民（含建筑师）对话与选项 90% 乱码（选项保留遗物图标）；商人对话 90% 乱码
- 第四轮：地图 Boss 点改显"?"（立绘/悬停信息移除）；快照标注字号加大、增亮描边、避让濒危提示
- 第五轮：奖励界面右键查看屏即使卡已揭示也按获得场景遮蔽（修复投掷匕首/连续反弹等泄漏）
- 第五轮：房间结束"前进"按钮与火堆选项文字 60% 乱码
- 第六轮（澄清）：**顶栏**阶段 Boss 图标彻底移除（含"boss，整片地区最强大的敌人"悬停描述）
- 第六轮：阶段切换横幅（"阶段3 荣耀"）60% 乱码
- 第七轮：**全局 UI 文本乱码**（0.2s 场景扫描 + 幂等共享元数据）：所有未单独规定的提示/介绍/按钮/结算/模式与角色选择/先古之民名称描述等默认 60%；设置与图鉴豁免
- 第七轮：地图 Boss 点改回"?"（字号略小于原图标，节点可点击）
- 第八轮：标题 logo 与开场 logo 回退为原版（撤销乱码文本替身）
- 第八轮：顶栏血量快照与金币数值回退为可见（仅描述文字乱码；数值不做全局扫描）
- 第八轮：药水槽位（含空槽）标题与描述统一 50% 乱码（改由悬停提示层处理）

### 泄漏修复（审计驱动）
- 未知卡不再显示真实悬停提示（关键词/机制）；事件选项/商店商品/遗物 Focus/遗物奖励同理
- 未知手牌不再显示"可打出"高亮（费用不泄漏）
- 牌组查看的 类型/费用/字母/稀有度 排序在有未知卡时回退获得序（图鉴豁免）
- 获得场景隐藏类型编码卡框（仅稀有度边框）
- 特殊卡奖励（小偷归还/任务卡）名称与提示替换为"未知卡牌"

### 其他
- 事件模糊幂等（刷新/重写后保持模糊，不双重模糊）
- 敌人换皮（SetUpSkin）后重遮蔽；揭示瞬间刷新在场同定义卡面
- 移除战斗视野圆形遮罩（实机反馈：迷雾过大）——战斗场景仅敌人被呼吸框遮蔽，其余全可见
- 濒危文案与"未知卡牌"接入 RitsuLib I18N（zhs/eng 嵌入表）
- `BRAINFOG_DEBUG=1`：F9 状态覆盖层；Entry 记录游戏程序集版本
- Core 纯函数下沉：MapFogRules/CardContextClassifier/MaskEligibility（+测试）

## 0.1.0（2026-09-19，夜间自动迭代首版）

首个功能版：按 `docs/spec/SPEC-consolidated.md`（Blind0.01~0.03）实现 BrainFog 全部 Phase 1–3 功能。

### 卡牌可见性（核心）
- 所有卡牌实例初始黑雾（插画/名称/描述/费用不可见）
- 打出一次后该实例揭示真牌面；升级（锻造/事件/遗物）一律揭示
- 按实例记忆（同名副本不共享）、单局记忆、随存档持久化（RitsuLib RunSavedDataStore，牌序对齐重绑，fail closed）
- 悬停/选中未知牌：仅边框+黑雾；牌堆界面已揭示显示真牌面
- 获得场景（奖励/商店）：仅稀有度边框；升级版显示"+"标记
- 图鉴不遮蔽；黑雾覆盖 UpdateVisuals 重显路径

### 状态快照
- HP/金币显示旧快照；休息刷新两者；商店/事件扣钱刷新金币；获得/治疗不刷新
- 真实 HP < 15% 最大生命：角色红边 + 状态栏"濒危：我感觉自己快死了"（战斗内外）

### 战斗遮蔽
- 敌人模型替换为呼吸方框（跟随 Spine 动画相位；恐惧模式重遮蔽；己方召唤物可见）
- 敌人意图仅每场战斗首回合（回合>1 永久锁定，防阶段重置）
- 药水插画仅轮廓
- 玩家视野圆形遮罩（CanvasLayer shader，跟随本地玩家）

### 战斗外遮蔽
- 遗物不可见（检视界面黑雾、奖励图标隐藏）；Boss 遗物三选一豁免
- 地图迷雾（当前/已走/下一层可见）+ 画线工具禁用
- 事件文本 75% 确定性模糊（Unicode 码点级、合法 BBCode 保留、固定不重掷）

### 工程
- 纯逻辑 Core + Godot 适配层分离；80 单测 + 补丁目标审计（防游戏升级漂移）+ 发布形态回归
- PatchGuard 异常边界（异常不穿入游戏主循环）；BRAINFOG_DEBUG=1 状态日志
- 仅单人（联机自动禁用）；仅依赖 RitsuLib 0.6.2；MIT
